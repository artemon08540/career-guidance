import React from 'react';
import { Typography } from '@mui/material';

const Specialities = () => (
  <div>
    <Typography variant="h4">Сторінка "Спеціальності"</Typography>
    <Typography>
      Тут буде відображено інформацію про доступні спеціальності.
    </Typography>
  </div>
);

export default Specialities;
